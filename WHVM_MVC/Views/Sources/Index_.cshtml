@model IEnumerable<WHVM_MVC.Models.Source>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SourceLabel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SourceDateBurned)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SourceDateRipped)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SourceLabel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SourceDateBurned)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SourceDateRipped)
        </td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.SourceId">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.SourceId">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.SourceId">Delete</a>
        </td>
    </tr>        
}
    </tbody>
</table>
